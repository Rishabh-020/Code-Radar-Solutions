int len(int num){
    int len=0
    while(num>0){
        // int N=num%10;
        // arr[i]=N;
        ++len;
        num/=10;
    }
    printf("%d",len);
}
int mountainPeak(int num){
    len(num);
}